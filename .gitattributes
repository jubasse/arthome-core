# Prettier fixes the file (endOfLine: lf), git stops it arriving in the first place.
# The one setting where a different default would produce a whole-file diff.
* text=auto eol=lf

*.png binary
*.jpg binary
*.webp binary
*.woff2 binary
