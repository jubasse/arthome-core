{
  "$schema": "https://json.schemastore.org/tsconfig",
  "_comment": [
    "Applications: storefront web/mobile/TV, studio web/mobile. And the NestJS services,",
    "which publish no .d.ts. READ BY TYPESCRIPT 6 AND BY TYPESCRIPT 7.",
    "",
    "An application emits no declarations and delegates resolution to its bundler: that",
    "is what separates it from lib.json, and the reason there are three files and not",
    "two. Merging them would force the five applications to override three options each",
    "— and an option overridden five times is not locked, it is a default.",
    "",
    "moduleResolution bundler — Next, Vite and Metro resolve for themselves. A repository",
    "whose tool requires nodenext (a NestJS service, say) redefines it locally: module and",
    "moduleResolution are both freely redefinable.",
    "",
    "Deliberately NOT here: jsx (only three stacks out of seven need it, and they declare",
    "it themselves) and types (stays [] ; Next adds [\"node\"] locally). No path-bearing",
    "option either, same as base.json.",
    "See architecture/code-conventions.md section 4.4.2."
  ],
  "extends": "./base.json",
  "compilerOptions": {
    "noEmit": true,
    "module": "preserve",
    "moduleResolution": "bundler"
  }
}
