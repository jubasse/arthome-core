{
  "$schema": "https://json.schemastore.org/tsconfig",
  "_comment": [
    "outDir, rootDir, include and exclude belong to the package, never to the",
    "shared tsconfig: a relative path written in an extended file would resolve",
    "from node_modules/@arthome/tooling/tsconfig/."
  ],
  "extends": "@arthome/tooling/tsconfig/lib.json",
  "compilerOptions": {
    "outDir": "./dist",
    "rootDir": "./src"
  },
  "include": ["src/**/*.ts"],
  "exclude": ["src/**/*.spec.ts", "src/**/*.test.ts"]
}
